#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ether-converter/bin/ether_converter" "$@"
  ret=$?
else 
  node  "$basedir/../ether-converter/bin/ether_converter" "$@"
  ret=$?
fi
exit $ret
